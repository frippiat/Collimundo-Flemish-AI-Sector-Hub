from django.apps import AppConfig


class LoginConfig(AppConfig):
    """! Django boilerplate code for the login app.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "login"
