from django.apps import AppConfig


class LoginConfig(AppConfig):
    """! Django boiler plate code for the login app
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "companies"
