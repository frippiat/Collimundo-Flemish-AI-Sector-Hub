from django.apps import AppConfig


class AdminsConfig(AppConfig):
    """! Django boiler plate code for the admins app
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'admins'
