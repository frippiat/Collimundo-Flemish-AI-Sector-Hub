from django.apps import AppConfig


class WidgetsConfig(AppConfig):
    """! Django boilerplate code for the widgets app.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "widgets"
