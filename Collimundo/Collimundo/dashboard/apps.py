from django.apps import AppConfig


class DashboardConfig(AppConfig):
    """! Django boilerplate code for the dashboard app.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "dashboard"
